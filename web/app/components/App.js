var React = require('react');
var MultipleChoice = require('./MultipleChoice');

class App extends React.Component {
    render () {
        return (
            <MultipleChoice />
        )
    }
}

module.exports = App;